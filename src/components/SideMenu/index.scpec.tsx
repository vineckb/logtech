describe('<SideMenu />', () => {
  test('title is visible when open is true');
  test('title is not visible when open is false');

  test('rendering correct items in menu');

  test('showing menu items with text when open is true');
  test('showing menu items without text when open is false');

  test('all menu items has icon');
});
